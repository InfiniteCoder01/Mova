#include <mova.h>

int main() {
  Mova::Window window = Mova::Window("Test");
  // setContext(window);

  // while (!isKeyPressed(Key::Escape)) {
  //   clear();
  //   nextFrame();
  // }
  // destroyWindow(window);
}
